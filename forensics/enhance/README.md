### Current progress: Done

A `drawing.flag.svg` is given. svg file is not like other graphics file, 
in that the codes for the lines and such are instructions in the file itself.

That means, we can open up in the text editor, and we found out that the flag is hidden in tspan inside there:
`picoCTF{3nh4nc3d_58bd3420}`